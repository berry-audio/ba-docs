info:
  name: mixer.alsa_mixer_volume
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseURL}}"
  body:
    type: json
    data: |-
      {
        "jsonrpc": "2.0",
        "method": "mixer.alsa_mixer_volume",
        "params":{
          "device":"hw:CARD=sndrpihifiberry,DEV=0"
        },
        "id": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: example
    request:
      url: "{{baseURL}}"
      method: POST
      body:
        type: json
        data: |-
          {
            "jsonrpc": "2.0",
            "method": "mixer.alsa_mixer_volume",
            "params":{
              "device":"hw:CARD=sndrpihifiberry,DEV=0"
            },
            "id": 5
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: content-type
          value: application/json; charset=utf-8
        - name: content-length
          value: "417"
        - name: date
          value: Sun, 09 Aug 2026 14:04:23 GMT
        - name: server
          value: Python/3.11 aiohttp/3.13.5
      body:
        type: json
        data: |-
          {
            "jsonrpc": "2.0",
            "id": 5,
            "result": [
              {
                "name": "Software",
                "description": "Software volume from DSP",
                "index": null,
                "type": "playback",
                "channels": 2,
                "range": {
                  "min": -100,
                  "max": 0,
                  "unit": "dB"
                },
                "muted": false
              },
              {
                "name": "Digital",
                "description": "HifiberryDacpAd - snd_rpi_hifiberry_dacplusadc",
                "index": 3,
                "type": "playback",
                "channels": 2,
                "range": {
                  "min": 0,
                  "max": 207,
                  "unit": "steps"
                },
                "muted": false
              }
            ]
          }
