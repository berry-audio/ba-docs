info:
  name: mixer.get_mute
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}"
  body:
    type: json
    data: |-
      {
        "jsonrpc": "2.0",
        "method": "mixer.get_mute",
        "id": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: example
    request:
      url: "{{baseURL}}"
      method: POST
      body:
        type: json
        data: |-
          {
            "jsonrpc": "2.0",
            "method": "mixer.get_mute",
            "id": 5
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: content-type
          value: application/json; charset=utf-8
        - name: content-length
          value: "44"
        - name: date
          value: Sun, 09 Aug 2026 14:03:28 GMT
        - name: server
          value: Python/3.11 aiohttp/3.13.5
      body:
        type: json
        data: |-
          {
            "jsonrpc": "2.0",
            "id": 5,
            "result": false
          }
