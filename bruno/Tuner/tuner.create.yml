info:
  name: tuner.create
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}"
  body:
    type: json
    data: |-
      {
        "jsonrpc": "2.0",
        "method": "tuner.create",
        "params":{
          "name":"",
          "frequency":1021
        },
        "id": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
