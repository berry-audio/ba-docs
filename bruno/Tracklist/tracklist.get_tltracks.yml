info:
  name: tracklist.get_tltracks
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}"
  body:
    type: json
    data: |-
      {
        "jsonrpc": "2.0",
        "method": "tracklist.get_tltracks",
        "id": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
