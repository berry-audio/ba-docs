info:
  name: storage.unmount_shared
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseURL}}"
  body:
    type: json
    data: |-
      {
          "jsonrpc": "2.0",
          "method": "storage.unmount_shared",
          "params": {
              "dev": "smb://192.168.10.204/varun"
          },
          "id": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
