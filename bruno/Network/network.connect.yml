info:
  name: network.connect
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseURL}}"
  body:
    type: json
    data: |-
      {
        "jsonrpc": "2.0",
        "method": "network.connect",
        "params" :{
          "ifname": "wlan0"
        },
        "id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
