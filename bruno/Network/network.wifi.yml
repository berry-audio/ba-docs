info:
  name: network.wifi
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}"
  body:
    type: json
    data: |-
      {
        "jsonrpc": "2.0",
        "method": "network.wifi",
        "params":{
          "rescan": false
        },
        "id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
