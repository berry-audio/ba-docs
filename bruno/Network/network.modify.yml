info:
  name: network.modify
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseURL}}"
  body:
    type: json
    data: |-
      {
        "jsonrpc": "2.0",
        "method": "network.modify",
        "params" :{
          "name": "Vodafone-647B"
        },
        "id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
