info:
  name: network.connect_wlan
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseURL}}"
  body:
    type: json
    data: |-
      {
        "jsonrpc": "2.0",
        "method": "network.connect_wlan",
        "params" :{
          "ssid": "Vodafone-647B",
          "password": "04917687576631#$"
        },
        "id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
