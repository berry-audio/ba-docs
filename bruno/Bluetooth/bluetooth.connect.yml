info:
  name: bluetooth.connect
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseURL}}"
  body:
    type: json
    data: |-
      {
        "jsonrpc": "2.0",
        "method": "bluetooth.connect",
        "params": {
          "address": "14:4E:FD:88:AE:B1"
        },
        "id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
