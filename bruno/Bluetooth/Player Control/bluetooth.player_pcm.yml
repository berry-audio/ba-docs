info:
  name: bluetooth.player_pcm
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/mopidy/bluetooth/rpc/"
  body:
    type: json
    data: |-
      {
        "jsonrpc": "2.0",
        "method": "bluetooth.player_pcm",
        "id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
