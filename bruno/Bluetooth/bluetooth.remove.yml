info:
  name: bluetooth.remove
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseURL}}"
  body:
    type: json
    data: |-
      {
          "jsonrpc": "2.0",
          "method": "bluetooth.remove",
          "params": {
              "address": "14:4E:FD:88:AE:B1"
          },
          "id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
