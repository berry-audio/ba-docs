info:
  name: bluetooth.adapter_get_state
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}"
  body:
    type: json
    data: |-
      {
          "jsonrpc": "2.0",
          "method": "bluetooth.adapter_get_state",
          "id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
