info:
  name: bluetooth.adapter_set_state
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}"
  body:
    type: json
    data: |-
      {
          "jsonrpc": "2.0",
          "method": "bluetooth.adapter_set_state",
          "params": {
              "state": false
          },
          "id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
